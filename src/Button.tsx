import SlButton from "@shoelace-style/shoelace/dist/react/button";

export type ButtonProps = {
  canDoSomething: boolean;
};

export const Button = (props: ButtonProps) => <SlButton>Button</SlButton>;
